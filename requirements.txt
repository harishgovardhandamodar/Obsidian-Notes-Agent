# --- LangChain Core (Version 0.3.x is required for new structure) ---
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0

# --- Ollama Integration ---
langchain-ollama>=0.2.0,<0.3.0

# --- Vector Database (ChromaDB) ---
# Requires specific grpcio and protobuf versions to avoid build errors
chromadb>=0.5.0,<0.6.0
grpcio>=1.60.0
protobuf>=3.20.0

# --- Embeddings & NLP ---
sentence-transformers>=2.7.0
transformers>=4.40.0

# --- PDF Loading ---
pypdf>=4.0.0

# --- Utilities ---
python-dotenv>=1.0.0